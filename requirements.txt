# ===== WEB FRAMEWORK & SERVER =====
Flask[async]>=3.1.2          # Core web server for API endpoints WITH ASYNC SUPPORT
flask-limiter==3.5.0  # Rate limiting for API security
flask-talisman==1.1.0 # HTTPS enforcement & security headers
gunicorn==21.2.0      # Production WSGI server

# ===== API & NETWORK =====
requests>=2.31.0      # HTTP requests for CVE feeds and external APIs
PyGithub==2.3.0       # GitHub API integration (knowledge storage)
scapy==2.5.0          # Network packet manipulation
validators>=0.22.0    # URL validation security
aiohttp>=3.9.0        # Async HTTP client for concurrent API calls
httpx>=0.25.0         # Modern HTTP client with async support

# ===== SEARCH & INTERNET ACCESS =====
duckduckgo-search>=4.1.0    # DuckDuckGo search API (no API key needed)
googlesearch-python>=1.2.0  # Google search backup
wikipedia-api>=0.6.0         # Wikipedia knowledge retrieval
beautifulsoup4>=4.12.0       # Web scraping for truth verification
selenium>=4.15.0             # Dynamic web content scraping (optional)

# ===== SECURITY =====
pycryptodome==3.20.0  # Cryptographic functions
PyJWT>=2.8.0          
bcrypt>=4.0.0

# ===== SYSTEM MONITORING =====
psutil==5.9.8         # System resource tracking

# ===== AI/ML CORE =====
sentence-transformers>=2.2.2  # Text embeddings
transformers>=4.36.2          # NLP models (includes DialoGPT, BERT, etc.)
#torch>=2.2.1                  # Deep learning framework
datasets>=2.14.0              # Hugging Face datasets for training
accelerate>=0.24.0            # Training acceleration
safetensors>=0.4.0            # Safe model serialization
tokenizers>=0.15.0            # Fast tokenization
umap-learn>=0.5.5            # UMAP for dimensionality reduction

# ===== VECTOR DATABASE & MEMORY =====
faiss-cpu>=1.7.4              # Vector similarity search for memory
chromadb>=0.4.18             # Vector database for conversation history
langchain>=0.1.0             # LLM chain management (optional but useful)

# ===== LEARNING & FEEDBACK SYSTEM =====
scikit-learn>=1.3.0          # ML utilities for feedback analysis
optuna>=3.4.0                # Hyperparameter tuning for model improvement
pandas>=2.1.0                # Data manipulation for training data
matplotlib>=3.7.0            # Visualization of learning progress

# ===== DATA PROCESSING =====
numpy>=1.26.4,<2.0.0         # Numerical computing - PINNED TO 1.x
tqdm>=4.66.2                 # Progress bars
Pillow>=10.1.0               # Image processing for multimodal AI
librosa>=0.10.1              # Audio processing (if needed)

# ===== CACHING & STORAGE =====
redis>=4.5.0                 # Redis-based caching
sqlalchemy>=2.0.0            # Advanced database ORM
alembic>=1.12.0             # Database migrations

# ===== MONITORING & LOGGING =====
wandb>=0.15.0                # Experiment tracking (optional)
tensorboard>=2.14.0          # Training visualization
python-dotenv>=1.0.0         # Environment variable management

# ===== OPTIMIZED TORCH INSTALL =====
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.1+cpu             # Lighter CPU-only PyTorch build
river>=0.19.0                # Incremental learning
feedparser>=6.0.10           # RSS/Atom feed parsing
